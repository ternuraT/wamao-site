# 瓦猫新生 · 操作备忘

## 一、日常改代码后同步云端（在自己的电脑上）

```powershell
npm run build          # 1. 验证：类型检查 + 打包，必须无报错
git add -A
git commit -m "说明这次改了什么"
git push               # 2. 推送到 GitHub
```

> `npm` / `node` 不在 PATH 时的替代路径：`E:\kimi\resources\resources\runtime\npm.cmd`

## 二、启动本地预览

```powershell
npm run dev            # 开发模式，改代码即时刷新，地址 http://localhost:7100
npm run preview        # 预览打包产物（需先 build）
```

## 三、新电脑首次运行

1. 装 Node.js（≥18）：https://nodejs.org 下载 LTS 版本
2. 装 Git（一路默认选项）：https://git-scm.com/download/win
3. 拉取代码并启动：

```powershell
git clone https://github.com/ternuraT/wamao-site.git
cd wamao-site
npm install
npm run dev
```

## 四、常见问题速查

| 现象 | 原因 | 解决 |
|---|---|---|
| `npm.ps1 cannot be loaded... scripts is disabled` | PowerShell 执行策略限制 | `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`，输入 Y 确认 |
| `git 不是内部或外部命令` | 没装 Git 或没重开终端 | 安装 Git 后**关闭重开** PowerShell |
| `destination path ... already exists` | 目录里已有同名文件夹 | 先删掉或改名旧的 `wamao-site` 文件夹再 clone |
| `Connection was reset`（clone/push 时） | 国内网络访问 GitHub 不稳定 | 多试几次；或改用微信传 zip（见第五节） |
| `npm run dev` 报找不到 package.json | 不在项目目录里 | 先 `cd wamao-site` |
| 浏览器打开是旧版本 | 缓存 | 按 Ctrl+F5 强制刷新 |

## 五、GitHub 不通时的备用方案（微信传 zip）

在**有代码的电脑**上生成干净副本（自动排除 node_modules）：

```powershell
git clone F:\wamao\wamao-site "$env:USERPROFILE\Desktop\wamao-site"
```

然后右键桌面上的 `wamao-site` 文件夹 → 发送到 → 压缩文件夹，用微信发给另一台电脑。

在**接收的电脑**上解压后执行：

```powershell
cd <解压路径>\wamao-site
npm install
npm run dev
```

## 六、离线展示（答辩/比赛现场）

提前在有网时执行 `npm install` 和 `npm run build`，之后项目目录可直接带到无网环境，用 `npm run preview` 运行。注意：**双击 dist/index.html 打不开**，必须经过 `npm run preview`。

## 七、仓库信息

- 仓库地址：https://github.com/ternuraT/wamao-site （当前为私有，比赛需要公开时在 Settings → Danger Zone → Change visibility 改为 Public）
- 主要命令回顾：`git add -A`（暂存）→ `git commit -m "说明"`（提交）→ `git push`（推送）→ `git pull`（拉取最新）
